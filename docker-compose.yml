services:
  bot:
    build: ./bot
    restart: unless-stopped
    env_file: ./bot/.env
    volumes:
      - ./data:/app/data

  web:
    image: nginx:alpine
    restart: unless-stopped
    ports:
      - "127.0.0.1:8001:80"
    volumes:
      - ./web:/usr/share/nginx/html:ro
